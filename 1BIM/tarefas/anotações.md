O import apresentado é em formato diferente da versaão do react

ES modules vs Common JS

- 'npm init -y'
- npm run dev

em um navegador: "localhost:(porta utilizada)"

"dev": "node --watch server.js"
 A flag " --watch" faz com que toda a mudança feita no sevidor seja atualizada e recarregada na pagina, por isso temos o :

 "start": "node server.js"
 Para que ao rodar de verdade, sem ser na etapa desenvolvimento o server não fique atualizando com cada mudança, sendo estavel


 fastify e express
 - npm install fatify no cmd
 - crie um .gitingore e inclua o "node_modules"

 extensao THunderbolt??